# .env.local - Multi-tenant VAPI Configuration

# VAPI Master Account (for initial setup and fallback)
VAPI_MASTER_API_KEY=your_master_vapi_api_key_here

# Database
DATABASE_URL="file:./dev.db"

# NextAuth (for future authentication)
NEXTAUTH_SECRET=your_nextauth_secret
NEXTAUTH_URL=http://localhost:3000

# Application Settings
NODE_ENV=development

# Optional: For production
VERCEL_POSTGRES_URL=your_vercel_postgres_url

# Note: Client-specific VAPI settings (API keys, phone numbers, assistant IDs) 
# are stored in the database per client, not in environment variables
